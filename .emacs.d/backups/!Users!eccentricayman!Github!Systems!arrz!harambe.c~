#include <stdio.h>
#include <stdlib.h>

int main() {
    short s  = 83;
    double darr[4];
    short *sp;
    
    printf("size of darr: %lu\n", sizeof(darr));
    printf("size of s: %lu\n", sizeof(s));
    printf("size of s: %lu\n", sizeof(sp));
    return 0;
}
